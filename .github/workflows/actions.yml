name: GitHub Actions Demo
run-name: ${{ github.actor }} is running Flutter Actions 🚀🐦
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Flutter action
        uses: subosito/flutter-action@v2.11.0
